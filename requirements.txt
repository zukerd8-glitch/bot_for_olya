# Core dependencies
aiogram==3.10.0
openai==1.12.0
python-dotenv==1.0.0
loguru==0.7.2

# Database
sqlalchemy==2.0.25

# Pydantic (используем версии с pre-built wheels)
pydantic==2.5.3
pydantic-core==2.14.6
pydantic-settings==2.1.0

# HTTP
aiohttp==3.9.1

# Async files (убрали, т.к. уже в зависимостях aiogram)
